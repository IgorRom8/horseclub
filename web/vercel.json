{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "if [ \"$(basename \"$PWD\")\" = \"web\" ] && [ -f ../package.json ]; then cd .. && (npm ci || npm install); else (npm ci || npm install); fi",
  "buildCommand": "npm run build"
}
